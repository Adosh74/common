interface Color {
    red: number;
    green: number;
    blue: number;
    yellow?: number;
}

const color: Color = {
    red: 10,
    green: 10,
    blue: 10,
    yellow: 10
}

console.log(color);

export default color ;
